# Audax 项目协作台 · 服务器装机流程（Windows）

> 按顺序从上往下做，每步做完打个勾。整套约 20-30 分钟。
> 卡在哪一步，就把那步的命令行报错截图发出来。

---

## 阶段 0 · 装机前确认（2 分钟）

- [ ] 选定一台**长期开机、接公司内网**的 Windows 电脑做服务器（2 核 / 4G 内存 / 10G 空闲硬盘即可）。
- [ ] 这台电脑能上网（装依赖需要联网；日常使用只走内网）。
- [ ] 记下这台电脑将来放系统的盘（本流程用 **D 盘**示例）。

---

## 阶段 1 · 装基础软件（8 分钟，只需一次）

- [ ] **装 Node.js 20 LTS**：浏览器打开 https://nodejs.org → 下载「LTS」版 → 双击 → 一路「Next」到底。
- [ ] **装 Git**：打开 https://git-scm.com/download/win → 下载 → 双击 → 一路默认。
- [ ] **验证**：按 `Win+R`，输入 `cmd` 回车打开命令行，逐条执行，都能显示版本号才算成功：
  ```
  node -v
  git -v
  ```
  > 若提示「不是内部或外部命令」，说明没装好或需重开命令行窗口；重装对应软件再试。

---

## 阶段 2 · 固定内网 IP（3 分钟，强烈建议）

- [ ] 在这台电脑命令行执行 `ipconfig`，记下当前「IPv4 地址」（如 `192.168.1.50`）。
- [ ] 到路由器管理后台，把这台电脑的 IP **绑定/固定**为该地址（不同路由器叫「IP 与 MAC 绑定」「静态 DHCP」）。
  > 目的：重启后地址不变，同事的收藏链接一直有效。做不了也能先用，但重启后可能要重新查 IP。

---

## 阶段 3 · 下载并启动系统（8 分钟）

在命令行里**逐行**执行：

- [ ] 进入 D 盘：
  ```
  d:
  ```
- [ ] 下载代码（会生成 `d:\audax-platform` 文件夹）：
  ```
  git clone -b claude/project-management-system-siumg2 https://github.com/JimmyLi09/Project-Management.git audax-platform
  ```
- [ ] 进入目录并安装、构建（首次 2-5 分钟，请耐心等）：
  ```
  cd audax-platform
  npm install
  npm run build
  ```
- [ ] 先试跑一次：
  ```
  npm start
  ```
- [ ] 看到 **Ready** 后，本机浏览器打开 **http://localhost:3000**，能看到登录页 = 成功。
  > 用 `pd` / 密码 `audax123` 登录试试（首次会要求改密码，可以先改）。确认没问题后，回命令行按 `Ctrl+C` 停掉，进入下一阶段配置常驻。

---

## 阶段 4 · 配置后台常驻 + 开机自启（5 分钟，强烈建议）

不做这步的话，一关命令行窗口系统就停了。命令行执行：

- [ ] 安装 pm2：
  ```
  npm install -g pm2 pm2-windows-startup
  pm2-startup install
  ```
- [ ] 启动并设为常驻（先确保在项目目录）：
  ```
  cd /d d:\audax-platform
  pm2 start npm --name audax -- start
  pm2 save
  ```
- [ ] 验证：执行 `pm2 status`，看到 `audax` 状态是 `online` 即可。
  > 以后开机会自动运行。常用：`pm2 logs audax`（看日志）、`pm2 restart audax`（重启）。

---

## 阶段 5 · 放行防火墙，让同事能访问（3 分钟）

- [ ] 控制面板 → **Windows Defender 防火墙** → 高级设置 → **入站规则** → 新建规则 →
      端口 → TCP → 特定本地端口填 `3000` → 允许连接 → 全部勾选 → 名称填 `Audax 3000` → 完成。
- [ ] 用**另一台**电脑/手机浏览器访问 **http://<服务器IP>:3000**（如 `http://192.168.1.50:3000`），能打开登录页 = 全公司可用。

---

## 阶段 6 · 备份设置（3 分钟，重要）

- [ ] 确认自动备份已生效：打开 `d:\audax-platform\data\backups\`，应能看到一个 `audax-年-月-日.db` 文件（每天自动生成，保留 30 天）。
- [ ] **异机备份（强烈建议）**：设一个系统环境变量让备份多存一份到 NAS/网盘同步盘——
      控制面板 → 系统 → 高级系统设置 → 环境变量 → 新建系统变量：
      变量名 `AUDAX_BACKUP_DIR`，值填目标文件夹（如 `E:\audax-backup` 或 `\\NAS\audax-backup`）→ 确定 →
      回命令行执行 `pm2 restart audax` 生效。

---

## 阶段 7 · 交付前验收核对表

- [ ] 服务器本机 `http://localhost:3000` 能打开登录页
- [ ] 另一台设备 `http://服务器IP:3000` 能打开
- [ ] `pm2 status` 里 `audax` 是 `online`
- [ ] 重启这台电脑后，不手动操作，系统能自动恢复访问（验证开机自启）
- [ ] `data\backups\` 里有当天的备份文件
- [ ] 用 `pd` 登录后，能进入「用户管理」创建一个测试账号

全部打勾 → 装机完成，交给 PD 建账号、发《使用小贴士》给同事，即可开始内测。

---

## 日常维护速记

| 场景 | 操作 |
|---|---|
| 更新到新版本 | 双击 `d:\audax-platform\scripts\update.bat` |
| 看运行状态 / 日志 | `pm2 status` / `pm2 logs audax` |
| 手动重启 | `pm2 restart audax` |
| 备份 / 迁移数据 | 复制整个 `d:\audax-platform\data` 文件夹 |
| 换服务器 | 新机器做完阶段 1-5,再把旧机器的 `data` 文件夹拷到新机器同目录,重启 |

> 技术摘要:Next.js 15 + SQLite 单机部署,默认端口 3000(改端口:`set PORT=8080 && npm start`),无需数据库服务器、无需公网。
