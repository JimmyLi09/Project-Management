# Audax 项目协作台 · 使用小贴士（内测同事版）

> 一页看懂怎么用。有问题问 PD 或在群里反馈。

---

## 1. 登录

- 打开浏览器，访问 IT 给的内网地址（形如 **http://192.168.1.50:3000**）。
- 用**账号名**或**邮箱** + 密码登录。
- **首次登录会要求改密码**，设一个自己的新密码即可。
- 忘记密码？找 PD 在「用户管理」里帮你重置。
- 右上角 **中 / EN** 按钮可随时切换中英文界面。左下角 🔒 可自助改密码。

---

## 2. 我是什么角色，能干什么

| 你的角色 | 你主要看/做 |
|---|---|
| **PD / BD（总监）** | 全部：建项目、派 PM、定难度积分、批决策、管账号和模板 |
| **销售 Sales** | 建项目、填售前/商业信息、开票收尾;生产内容只能看 |
| **PM（项目经理）** | 自己负责项目的排期、信息清单、周报;勾选完成、派活给成员 |
| **成员 Member** | 只看「我的待办」里派给你(👤)的任务,做完打勾 |
| **只读 Viewer** | 什么都能看,不能改 |

左边栏就是主要功能区，从上到下：总览 / 项目 / 团队负载 / 我的待办 / 向上汇报 / 统计报表（PD/BD 还有 用户管理 / 模板管理）。

---

## 3. 最常用的几件事

**▸ 开工第一眼看哪** —— 点左上「总览」：三个大数字（进行中项目 / 按期率 / 未解决风险），下面是项目看板、团队负载、我的待办、待决策。右上角**铃铛**有红点=有逾期，点开看。

**▸ 新建项目（销售 / PD / BD）** —— 右上角「＋ 新建项目」→ 填名称、选服务（可多选，如 CGI+动画）、客户、负责 PM、起始日和交付日 → 创建。系统会**自动按模板生成排期和信息清单**，不用从零填。

**▸ 更新排期进度（PM / 成员）** —— 进项目 →「排期」标签：
- 每个阶段左边**打勾 = 完成**；
- 点右边**状态徽章**切换「未开始 → 进行中 → 已完成 → 受阻」；
- 🔒 图标是**冻结点**（确认后锁定，改动会影响后面所有阶段，务必先跟客户确认）；
- 未打勾又过了到期日 = **逾期**（标红）。

**▸ 记录客户资料到位没（PM）** —— 进项目 →「信息清单」：每项点状态徽章推进「未收到 → 已收到 → 已确认」；可上传截图存证；不适用的选 N/A（不算进完成率）。

**▸ 我今天该做什么（所有人）** —— 左栏「我的待办」：按到期日排序，逾期标红。成员只会看到派给自己(👤)的任务，做完打勾即可。侧栏「我的待办」旁的数字就是你未完成的条数。

**▸ 每周汇报（PM 必做）** —— 左栏「向上汇报」找到你的项目，填这六格：本周完成 / 下周关键节点 / 当前风险 / 需 Director 决定 / 客户待确认 / 预算变更。点一下格子就能编辑。**Director 每周会统一看**，超过 7 天不更新会标黄提醒。

**▸ 需要总监拍板** —— 在周报「需 Director 决定」里写清楚，PD 会在向上汇报页直接**批准 / 需补充 / 驳回**，结果你能看到。

---

## 4. 特殊情况

- **有人休假 / 离职** —— PD/BD 在「团队负载」页点某人的「转交全部」，把 TA 名下所有项目一次性转给别人；离职再到「用户管理」停用账号。
- **项目做完了** —— 完成后可在项目页「📦 归档」，它就从各个工作视图里收起来（记录仍保留，随时能在「项目」页的「已归档」里翻出来）。
- **导出给客户 / 存档** —— 项目页「导出」→ 选中英文和要显示的栏位 → 打印或另存 PDF。
- **想看某项目从头到尾谁改过什么** —— 项目「概览」→「完整历史」，永久记录。
- **两个人同时改同一处** —— 以后保存的人为准；如果你的改动覆盖了别人刚存的，底部会弹提示让你刷新核对。

---

## 5. 给内测的小提醒

- 先挑 **2-3 个真实在跑的项目**录进来试，别一次把所有项目搬进来。
- **姓名要统一**：项目里的负责人和派活都是按「姓名」匹配的，账号姓名和平时叫法保持一致。
- 数据是**大家共享**的，别人几秒内就能看到你的更新（页面每 30 秒自动刷新，也可手动刷新）。
- 用着别扭、缺功能、有 bug，随时反馈——内测就是用来收集这些的。

祝用得顺手 🎉
