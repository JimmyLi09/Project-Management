# 上线操作单 — 2026-08-09 全量版

本次把 **REQ-001 ~ REQ-019 共 19 条需求**一次性推到 B2 内网服务器。
数据库结构是**增量变更**（只新增表，不改不删旧表旧字段），老数据原样保留。

---

## 一、上线前（5 分钟）

1. **挑个没人用的时段**（午休或下班后）。整个过程约 2–5 分钟，期间页面打不开。
2. **确认服务器能上外网**（要从 GitHub 拉代码）。
3. **手动多备一份**（可选但推荐）：把整个 `data` 文件夹复制到 U 盘或 NAS。
   > 更新脚本本身会自动做一份带时间戳的快照（见下），这一步是双保险。

## 二、执行更新（1 条命令）

在服务器上进到项目目录，然后：

- **Windows**：双击 `scripts\update.bat`
- **Mac / Linux**：`bash scripts/update.sh`

脚本会依次做：

| 步骤 | 说明 |
|---|---|
| ① 更新前快照 | 存到 `data/backups/pre-update-<日期时间>.db` —— 回滚就靠它 |
| ② 拉最新代码 | `git reset --hard origin/main`（`data/` 在 .gitignore 里，不会被动） |
| ③ 装依赖 + 构建 | 约 1–3 分钟 |
| ④ 重启服务 | 有 pm2 就自动重启，没有会提示手动重启 |

看到 `√ 更新完成` 即成功。

## 三、上线后自查（5 分钟，用 pd 账号）

按顺序点一遍，任何一条不对就先回滚再找我：

- [ ] 能登录，左侧项目列表数量和昨天一致
- [ ] 随便开一个老项目 → 排期页正常显示，阶段/日期没乱
- [ ] 同一项目 → 信息清单页正常显示，已确认/已收到的状态还在
- [ ] 项目详情右上角出现 **「⧉ 复制项目」** 按钮（本次新增）
- [ ] 排期页、清单页各出现一条 **「排期来源 / 清单来源」** 工具条（本次新增）
- [ ] 清单页点「增减信息项」→ 每行右侧有 **⠿** 拖动手柄，能拖动换顺序
- [ ] 点「导出」→ 能出预览，页码/页眉正常，A4 横纵可切
- [ ] Project Registers（7 张登记表）能打开，老记录还在
- [ ] 换一台员工电脑，用 `http://<服务器IP>:3000` 能正常打开

## 四、回滚（万一出问题）

```bash
# 1. 停服务
pm2 stop audax

# 2. 用更新前的快照覆盖回去（文件名换成 data/backups/ 里最新那个 pre-update-*.db）
cp data/backups/pre-update-20260809-XXXXXX.db data/audax.db
rm -f data/audax.db-wal data/audax.db-shm

# 3. 代码退回上一个版本
git reset --hard HEAD~1
npm install && npm run build

# 4. 起服务
pm2 start audax
```

Windows 同理，`cp` 换成复制粘贴，`pm2` 命令一样。

---

## 五、本次新增了什么（给同事的一句话说明）

| 你会看到 | 说明 |
|---|---|
| 项目可以「复制」了 | 右上角 ⧉ 复制项目，可整项目复制 / 只复制排期 / 只复制清单。副本是全新编号、进度清零 |
| 排期和清单能互相「抄」 | 两个页面顶部的来源工具条：从别的项目导入，或存成模板下次直接套 |
| 清单能拖着排序了 | 进「增减信息项」，拖右侧 ⠿ 手柄 |
| 清单可以不分分类 | 勾「无固定分类」，变成平铺的 信息项/状态/日期/备注 |
| 排期有两种参考版式 | 按服务分组（周）／按日期（Scale Model） |
| 导出更好用了 | 可选导出内容、排序、A4 横纵、页码页眉、公司说明 |
| 总览 KPI 能点进去 | 点数字直接下钻到对应项目列表 |

## 六、注意事项（沿用）

- 平台**只在公司内网**跑，不要做端口映射暴露到公网。
- 防火墙**只开 3000 端口**，不要整个关掉。
- `data/` 文件夹是全部家当，不要删、不要直接改里面的 `.db` 文件。
- 内置每日自动备份留 30 天，在 `data/backups/`；建议再设 `AUDAX_BACKUP_DIR` 指向 NAS 做异机备份。
