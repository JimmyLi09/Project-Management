# 0806 批 — 开发顺序与依赖（索引）

## 建议开发顺序与依赖（2026-08-06 · Cowork 排定）

> 本批 10 条无 P0 Bug（均 P1/P2 增强），按「快赢 + 依赖」分 5 阶段。REQ-005 为上批遗留，一并纳入首阶段。

### Phase A — 快赢 / 独立（先做）
1. **REQ-005** 编辑体验（JobRecord 表格化+总编辑、信息清单只读→编辑、备注多行）— 遗留项；「只读→编辑」范式被 REQ-010/019 复用，先立
2. **REQ-010** 新建表单增删分区 + 公司联系人
3. **REQ-011** 总览 KPI 下钻
4. **REQ-016** 导出公司说明 / 免责声明（小、独立）

### Phase B — 导出基座
5. **REQ-017** 导出版式（编号重排 / 分页 / 页码 / 页眉页脚 / A4 横纵）— 导出地基，先修
6. **REQ-015** 灵活导出（选内容 + 排序 + 统一模板）— 依赖 REQ-017

### Phase C — Checklist 重构（互相依赖，连着做）
7. **REQ-014** 分类自定义（先，影响清单结构）
8. **REQ-013** Checklist 自动状态 + 空白项导出
9. **REQ-019** Checklist 套参考模板（列 / 分组 / 文件名+日期）— 依赖 REQ-014、REQ-013

### Phase D — Schedule 模板
10. **REQ-018** Schedule 套参考模板（按服务分组周 / 按日期两种样式、日期、小计/Sum、可编辑）— 独立成阶段

### Phase E — 复制 / 模板（最后）
11. **REQ-012** 复制项目 / 模板导入导出 + Checklist 拖动 — 依赖 REQ-018、REQ-019 模板结构就位

### 验证插项
- **REQ-007** 导出拆分：之前部署 bug 已修，需在最新线上复查确认。

### 依赖主线
- Checklist 三件套 **14 → 13 → 19** 打包做；导出两件套 **17 → 15** 打包做；Schedule 模板 **18** 独立；复制/模板 **12** 压最后。
